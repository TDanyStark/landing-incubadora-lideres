---
interface Props{
  text: string;
  color?: string;
  weigth?: string;
}

let { text, color, weigth } = Astro.props;
color = color || 'text-accent';
weigth = weigth || 'font-bold';
---

<h2 class={`${color} font-bold text-4xl md:text-6xl text-pretty`}>{text}</h2>